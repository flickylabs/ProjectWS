R14: PASS — 빌드/타입 검증
  상세: 최종 코드 기준 `npx tsc -b --force` 통과. `npm run build`도 통과했다.
  문제: build output에 기존 CSS `@import` 순서 경고, dynamic import 비효율 경고, chunk size 경고가 남아 있다. 이번 수정으로 생긴 TypeScript/build 실패는 없다.
  수정: 없음.
  severity: none
