R10: PASS — Phase/강제 전환 정합성
  상세: 턴 증가 후 readiness를 갱신하고 현재 `turnCount`로 forced verdict를 검사한다(`src/store/slices/phaseSlice.ts:68-90`). 판결 가능 조건은 turn, readinessScore, progressedDisputes, majorBreakthroughs 기반이다(`src/engine/readinessEngine.ts:68-86`). 강제 전환은 기본 16턴 + hidden emergence 보너스 4턴 구조다(`src/engine/readinessEngine.ts:18-27`, `src/engine/readinessEngine.ts:97-107`).
  문제: 수정 전 hidden reveal 집계가 `state.discoveredTruths`를 보도록 되어 있어 항상 0에 가까웠고, reload 후 `_emergenceCount` 손실 시 보너스턴이 줄 수 있었다.
  수정: hidden reveal을 `discovery.disputeVisibility`의 emerged/emergedAtTurn 기준으로 집계하고(`src/store/useGameStore.ts:653-655`), forced max turn도 readiness의 `hiddenDisputeRevealCount`를 함께 반영하도록 보정했다(`src/engine/readinessEngine.ts:103-106`).
  severity: none (high fixed)
