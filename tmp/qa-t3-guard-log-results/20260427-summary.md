# QA-T3-GuardLog Summary

- Session: QA-T3-G-L
- Date: 2026-04-27
- Scope: Tier 3 disclosure guard `?guard=log` / localStorage log mode detection verification only
- Write scope used: this summary file only, under `tmp/qa-t3-guard-log-results/`
- Sanitization/block execution: not executed
- Full playthrough: not executed; no full playthrough harness available

## Start Gate

- `git status --short --branch`: `## main...origin/main` plus existing untracked `tmp/REQUEST-*`, `tmp/qa-*`
- `git log --oneline -1`: `f03f49f feat(engine): tier-3 disclosure guard MVP — log mode + feature flag default off`
- tracked clean check: PASS. Initial tracked clean was confirmed by exit-code check. `npm run check:all` generated two tracked tmp precheck outputs during the run; those generated changes were restored to preserve read-only/tracked-clean state.
- `npm run check:all`: PASS, hard 0, warnings 157
- `npm run build`: PASS
- `npx tsc -b --force`: PASS
- baseline tags present: `baseline-pre-policy-v1`, `baseline-pre-policy-v2`
- final tracked state after QA: tracked clean

## Result Stats

| Area | Test type | Runs | Guard mode | Result |
|---|---:|---:|---|---|
| 1. mode=off sample | Unit/resolver smoke | 63 | off | PASS |
| 2. mode=log sample | Unit/resolver smoke | 63 + localStorage 5 | log | FAIL, 1 unique FN |
| 3. case targeted sample | Unit/resolver smoke | 210 | log | PASS |
| 4. gating matrix | Scripted data matrix audit | 30 | log | PASS |
| 5. scripted path inspection | Static scan | 1 path audit | off/log applicability | PASS |
| 6. Dev-A corrected area | Scripted data matrix audit | 23 entries | log applicability | PASS with note |
| 7. Browser/UI headless | Browser/UI headless | 0 | log | BLOCKED |
| 8. Full playthrough simulation | Full playthrough | 0 | log | BLOCKED |
| Manual play spot check | Manual play spot check | 0 | log | BLOCKED |

## 1. mode=off sample

- Test type: Unit/resolver smoke
- Runs: 63 samples, derived from Dev-A A-1 truth-guard before texts excluding the 2 D6 register-only entries
- Seeds: none; deterministic matrix
- Entrypoint: Vite SSR in-memory module load, `blockHiddenTruthLexemes(text, context)`; no new harness file created
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: `off` via `?guard=off`
- Result: PASS
- Observed: 63/63 returned `{ action: 'pass' }`
- Warnings: 0
- Policy import/scan side-effect check: policy cache stayed null for `family-01`, `friend-01`, `spouse-01` before and after off-mode calls
- FP/FN candidates: none

## 2. mode=log sample

- Test type: Unit/resolver smoke
- Runs: 63 URL-mode samples plus 5 localStorage-mode samples
- Seeds: none; deterministic matrix
- Entrypoint: Vite SSR in-memory module load, `ensureDisclosurePolicyLoaded(caseId)`, then `blockHiddenTruthLexemes(text, context)`
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: `log` via `?guard=log`; localStorage key `solomon-disclosure-guard-mode=log` separately checked
- Result: FAIL due to 1 unique false negative candidate
- URL-mode observed: 60/63 returned `action='log'`; 3/63 returned `pass`
- localStorage-mode observed: 5/5 returned `action='log'`
- Warns emitted: 60 URL-mode disclosure guard warnings, matching the 60 URL-mode `log` results
- Matched examples: `유서를 손댄`, `자기 몫을 줄`, `어머니 통장으로 꾸준히 돈을 넣었습니다`, `제 형 쪽 생활이 그렇게 이어진 정황`, `공장 자금`, `선을 넘는 메시지`, `거절했다는`

False negative candidate:

| Case | Variant | Channel | Expected | Actual | Candidate reason |
|---|---|---|---|---|---|
| family-01 | `judgeq-d-3-fact_pursuit-1-v1` | `judge_question` | `log` | `pass` | Source text has `어머니 통장으로 정기적으로 돈을 보내신`; guard rule catches `어머니 통장으로 정기적으로 돈을 보낸`, so the honorific inflection `보내신` is missed. |

## 3. case별 targeted sample

- Test type: Unit/resolver smoke
- Runs: 210 samples
- Seeds: none; deterministic matrix
- Entrypoint: `blockHiddenTruthLexemes(text, context)` with parsed inline paraphrase labels from `src/engine/disclosureGuard.ts`
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: `log`
- Result: PASS
- Observed: 198 expected `log`, 12 expected `pass`, 0 mismatches

Coverage:

- `family-01`, `friend-01`, `spouse-01` paraphrase labels were run through `judge_question` and NPC `interrogation` S0/S1/S2.
- `혈연이 다른` logs; `혈연` alone passes.
- `같은 패턴 반복` logs; `같은 패턴` alone passes.
- `뜯` alone passes; `뜯` with father context logs.
- `다은이 아버지가 ... 가져간` regex logs; incomplete regex input passes.
- `거절했다는` / `거절한 사실` log only in `judge_*`, `system_message`, `dossier`, `evidence_discovery`; NPC natural `interrogation` passes.

FP/FN candidates: none from this matrix.

## 4. gating 영역 검증

- Test type: Scripted data matrix audit
- Runs: 30 contexts
- Seeds: none; deterministic matrix
- Entrypoint: `blockHiddenTruthLexemes(text, context)` using `family-01` tripwire lexeme from policy JSON
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: `log`
- Result: PASS
- Observed: 16 expected `log`, 14 expected `pass`, 0 mismatches

Blocked as `log`:

- `judge_question`, `judge_contradiction`, `judge_evidence_combo`, `judge_witness_summon`
- `system_message`
- `dossier` omitted/early
- `evidence_discovery`
- NPC `interrogation` / `contradiction_pursuit` at S0/S1/S2
- `evidence_present` with `investigationStage` 0/1

Passed:

- `aftermath`, `emotional_overload`, `mediation`, `witness_full`
- NPC `interrogation` / `contradiction_pursuit` at S3/S4/S5
- `dossier` mid/late
- `evidence_present` with `investigationStage` 2/3

FP/FN candidates: none from this matrix.

## 5. tryScriptedDialoguePath flow area

- Test type: Static scan
- Runs: 1 static path inspection
- Seeds: none
- Entrypoint: `rg` path inspection on `src/engine/llmDialogueResolver.ts`, `src/engine/disclosureGuard.ts`, `src/engine/disclosurePolicyLoader.ts`
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: off/log applicability only
- Result: PASS

Findings:

- `tryScriptedDialoguePath(...)` is called before guard hooks.
- `if (scriptedResult !== null) return scriptedResult` returns immediately, so successful ScriptedText does not enter guard.
- Guard hooks exist after scripted miss for blueprint result, blueprint judge question, blueprint fallback judge question, LLM judge question, LLM NPC response, and legacy fallback paths.
- `applyDisclosureGuardToText(...)` returns early when mode is `off`; otherwise it loads policy and calls `blockHiddenTruthLexemes`.

## 6. Dev-A 보정 영역 정합

- Test type: Scripted data matrix audit
- Runs: 23 Dev-A A-1 entries
- Seeds: none
- Entrypoint: read-only JSON traversal of `src/data/scriptedText/{family-01,friend-01,spouse-01}.json`; static path inspection from area 5
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: log applicability only; runtime guard is not entered for successful scripted path
- Result: PASS with note
- Observed: 23/23 current ScriptedText entries exactly match Dev-A A-1 `After` text; 0/23 still equal the old `Before` text
- Interpretation: Dev-A corrected entries are present in ScriptedText. Because successful `tryScriptedDialoguePath` returns before guard hooks, runtime guard non-firing is intentional and matches option (가), LLM/Fallback only.

Note for future Tier 3.5+:

- If a future final-text guard scans ScriptedText directly, `friend-01` `discover-b-e-4-capture-v1` current corrected text still logs on lexical `선을 넘는 메시지`.
- This is not a current runtime FP because ScriptedText bypasses guard by design. It is a future FP candidate if the guard scope expands to scripted final text without quoted/reported-speech nuance.

## 7. Browser/UI headless

- Test type: Browser/UI headless
- Runs: 0
- Seeds: none
- Entrypoint attempted: availability check only
- UI opened: no
- Phase/action progressed: no; not a full playthrough
- Guard mode: log target, not executed in browser
- Result: BLOCKED

Blocker evidence:

- `tests/run-84-headless.cjs` is missing.
- `node_modules/playwright` exists, but `node_modules/@playwright/test` is missing.
- Existing `tmp/playwright-smoke.spec.js` is a generic title/body smoke, not a guard-log spouse progression harness.
- Creating a new guard-log headless harness is outside this QA session's allowed scope.

## 8. Full playthrough simulation

- Test type: Full playthrough
- Runs: 0
- Seeds: none
- Entrypoint attempted: availability check only
- UI opened: no
- Phase/action progressed: no
- Guard mode: log target, not executed in full playthrough
- Result: BLOCKED
- Required statement: BLOCKED: no full playthrough harness

## Manual play spot check

- Test type: Manual play spot check
- Runs: 0
- Seeds: none
- Entrypoint attempted: none
- UI opened: no
- Phase/action progressed: no
- Guard mode: log target, not executed manually
- Result: BLOCKED
- Reason: no interactive/manual QA harness was available in this read-only session, and no new harness was created.

## FP / FN Candidates

False positive candidates:

| Status | Case | Variant | Context | Detail |
|---|---|---|---|---|
| Future-scope only | friend-01 | `discover-b-e-4-capture-v1` | direct guard on current ScriptedText after text | Direct call logs `선을 넘는 메시지`, but runtime scripted path bypasses guard. If Tier 3.5+ scans scripted final text, reported-speech nuance is needed. |

False negative candidates:

| Status | Case | Variant | Context | Detail |
|---|---|---|---|---|
| Current log-mode FN | family-01 | `judgeq-d-3-fact_pursuit-1-v1` | `judge_question`, source before text direct call | `어머니 통장으로 정기적으로 돈을 보내신` passes because current matcher catches `...보낸` but not `...보내신`. |

## Gap Report

- Static scan: available and executed.
- Unit/resolver smoke: available and executed with Vite SSR in-memory loading; no harness file created.
- Scripted data matrix audit: available and executed read-only.
- Browser/UI headless: blocked; no approved guard-log browser harness.
- Full playthrough simulation: blocked; `tests/run-84-headless.cjs` missing.
- Manual play spot check: blocked; no manual/UI harness in this session.

Recommended separate Codex-Dev request:

- Build or restore a read-only-compatible guard-log UI/full-playthrough harness that can launch `?guard=log`, progress `spouse-01` through real phase/action steps, and assert `console.warn('[disclosure-guard]', ...)` without writing outside the assigned result directory.

