# QA Functional Result — Plan P-5

- **Session**: QA-Codex-Functional / Thread-Q 재진입 v4
- **HEAD**: db0130e (`docs(qa): tier-2 QA test cases — functional / scripted / index`). Baseline anchor: a10b8011c3311d2a6ab20dd4a06edb29a4ac48e3
- **Scope**: P-5 only / TC-I2 archetype voice LLM sample readiness check. P-1 not retried; P-3/P-4 not reprocessed.
- **Commands run**: `git status --short --branch`; `git log --oneline -1`; `git diff --quiet && git diff --cached --quiet`; `npm run check:all`; `rg "TC-I2|P-5|archetype..." docs/qa-functional.md CLAUDE.md memory/story_v2_confirmed_3cases.md`; `Get-Content -Raw -Encoding utf8 memory/story_v2_confirmed_3cases.md`; API/env presence check without printing secret values; Node dotenv `/v1/models` connectivity check; static inspection of `src/engine/llmClient.ts`, `src/engine/llmDialogueResolver.ts`, `src/engine/blueprintPromptBuilderV2.ts`, active case JSON archetype fields, and existing `tmp/codex-recovery-v6/precheck-archetype-quant.*`
- **PASS / FAIL / BLOCKED**: 0 / 0 / 1. P-5 BLOCKED before sampling.
- **P0 findings**: 0
- **P1 findings**: 0 new P-5 findings
- **P2 findings**: 0 new P-5 findings
- **Known baseline warnings**: 157 policy warnings, plus 1 known `run-all-checks.cjs` warn (`legacy-precheck-matrix... 10 known family/friend d-5 HIGH issues`)
- **New warnings**: 0
- **New hard issues**: 0
- **Warning delta explanation**: Start-condition `npm run check:all` returned exit 0, wrapper hard 0, policy hard 0, policy warnings 157, sync issues 0. No wrapper regression observed.
- **Repro seed / variant ID / TC ID**: TC-I2 / P-5. No sampling seed was consumed because sampling was blocked. Planned seed list would be `p5-{archetype}-{001..100}` per NPC after a proper harness exists.
- **Suggested next action**: Codex-Dev should add a P-5 sampling harness that can invoke the real `resolveLLMDialogue`/Blueprint LLM path under controlled store state, bypass or account for ScriptedText priority intentionally, load `.env` safely, and persist only sanitized metrics. CT-Main should decide whether P-5 must test runtime LLM path or a separate prompt-only archetype harness.
- **Files touched**: none, except this QA report under `tmp/qa-functional-results/`.

## P-5 Added Fields

- **Sample 수**: 0 actual LLM samples. Planned target was 6 NPC/archetype profiles × 100 seeds = 600 responses.
- **Seed list**: Not consumed. Planned deterministic format: `p5-victim_cosplay-001..100`, `p5-avoidant-001..100`, `p5-confrontational-001..100`, `p5-affect_flattening-yun-001..100`, `p5-premature_summary-001..100`, `p5-affect_flattening-choi-001..100`.
- **평가 기준**:
  - `victim_cosplay` / 박지연: 단정·피해자 프레임 marker rate >= 70%. Candidate markers: `저는`, `저만`, `억울`, `당한`, `왜 저만`, `제가 뭘`.
  - `avoidant` / 이준호: S0~S2 모호어 marker rate >= 50%. Candidate markers: `그 부분`, `순서`, `조심`, `말씀드리기 어렵`, `복잡`, `지금 당장`, `그 정도`.
  - `affect_flattening` / 윤정후, 최수민: 격앙 marker rate <= 10%. Candidate escalation markers: `!`, `왜`, `말도 안`, `화가`, `분노`, `억울`, `도대체`.
  - `confrontational` / 윤태성: 직접 공격·역공 marker count high, with attack directed to judge-facing speech. Candidate markers: `오히려`, `분명`, `억지`, `묻으셔야`, `문제입니다`, `틀렸습니다`.
  - `premature_summary` / 송다은: conclusion-jump marker count present. Candidate markers: `결론`, `정리하면`, `그러니까`, `핵심은`, `중요한 건`, `전체 맥락`.
- **BLOCKED 사유**:
  - API key status: process env has no `OPENAI_API_KEY` or `VITE_OPENAI_API_KEY`; `.env` contains `VITE_OPENAI_API_KEY`. Secret value was not printed.
  - API connectivity: Node with `dotenv` loaded `.env` and `GET https://api.openai.com/v1/models` returned HTTP 200.
  - Attempted call path: inspected `src/engine/llmClient.ts` (`VITE_OPENAI_API_KEY` -> OpenAI `/chat/completions`, otherwise local LM Studio) and `src/engine/llmDialogueResolver.ts`.
  - Blocking point: no existing P-5 LLM sampling harness was found. Active question/evidence runtime first enters `tryScriptedDialoguePath`, which returns ScriptedText for active cases before LLM. A valid P-5 run needs a controlled harness to invoke the real LLM/Blueprint path, or a CT-approved prompt-only harness. Creating that harness is outside QA detection/report scope.
  - Additional context source issue: requested `memory/story_v2_confirmed_3cases.md` is absent in this workspace. Archetype mapping was confirmed from active case JSON instead.
- **known finding vs new finding**:
  - known: P-3 PASS from prior report; P-4 FAIL findings accepted by CT-Main (`soft/mid/hard` spec mismatch, `targetParty:both` dead variants).
  - new: no P-5 archetype voice mismatch was sampled or found because P-5 is BLOCKED.

## Confirmed Archetype Map

- spouse-01 / partyA / 박지연: `victim_cosplay`
- spouse-01 / partyB / 이준호: `avoidant`
- family-01 / partyA / 윤태성: `confrontational`
- family-01 / partyB / 윤정후: `affect_flattening`
- friend-01 / partyA / 송다은: `premature_summary`
- friend-01 / partyB / 최수민: `affect_flattening`
