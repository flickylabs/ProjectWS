# QA Functional Result — Plan P-3/P-4

- **Session**: QA-Codex-Functional / Thread-Q 재진입 v3
- **HEAD**: db0130e (`docs(qa): tier-2 QA test cases — functional / scripted / index`), CT-Main 갱신 기준. Baseline anchor: a10b8011c3311d2a6ab20dd4a06edb29a4ac48e3
- **Scope**: Start conditions; P-1 blocked confirmation; P-3 / TC-J2 `npm run check:all` regression stress; P-4 / TC-A3 judge question static matrix audit + 500 seeded distribution sample. P-5 not run; P-4 already produced actionable static findings and no LLM sample was required.
- **Commands run**: `git status --short --branch`; `git log --oneline -1`; `git diff --quiet && git diff --cached --quiet`; `npm run check:all` initial baseline; `Get-Content -Raw -Encoding utf8 docs/qa-functional.md docs/disclosure-policy.md baseline/pre-policy-v1/rollback-procedure.md CLAUDE.md`; `Get-ChildItem -Name tests`; `npm run check:all` repeated 15 times; inline Node static audit for `src/engine/judgeQuestionEngine.ts`, `src/engine/scriptedTextLoader.ts`, `src/types/renewal.ts`, `src/data/scriptedText/{spouse-01,family-01,friend-01}.json`
- **PASS / FAIL / BLOCKED**: PASS 1 / FAIL 1 / BLOCKED 1. P-3 PASS; P-4 FAIL with static findings; P-1 BLOCKED because `tests/` directory / `tests/run-84-headless.cjs` is absent.
- **P0 findings**: 0
- **P1 findings**: 1 — TC-A3 / P-4: documented tone/depth matrix does not match current engine matrix. `docs/qa-functional.md:99` requires `4 type × 4 depth × soft/mid/hard`, and `CLAUDE.md:261` defines `soft/mid/hard`. Current engine has `type Tone = 'soft' | 'hard'` at `src/engine/judgeQuestionEngine.ts:15`, and `evidence_present` is clamped to max depth 2 at `src/engine/judgeQuestionEngine.ts:269`. Static engine pool still totals 84 templates, but it is not the documented 4×4×3 tone/depth matrix.
- **P2 findings**: 1 — TC-A3 / P-4 distribution skew: active `judge_question` scripted data contains 168/720 variants tagged `targetParty:both`, but `scoreVariant` treats any tag other than exact `a`/`b` as mismatch (`src/engine/scriptedTextLoader.ts:179-180`). Seeded 500-sample static model selected `targetParty:both` 0 times. Repro seed: `P4-static-db0130e-20260427`.
- **Known baseline warnings**: 157 policy warnings, plus 1 known `run-all-checks.cjs` warn (`legacy-precheck-matrix... 10 known family/friend d-5 HIGH issues`)
- **New warnings**: 0 from P-3 repeated wrapper runs. P-4 findings are static QA findings, not `check:all` warning deltas.
- **New hard issues**: 0 from `npm run check:all`
- **Warning delta explanation**: Initial `check:all` and all 15 repeat iterations returned exit 0, policy hard 0, policy warnings 157, sync issues 0. No baseline warning count drift observed.
- **Repro seed / variant ID / TC ID**: TC-A3 / P-4 / seed `P4-static-db0130e-20260427`; selected distribution: case `friend-01` 161, `spouse-01` 167, `family-01` 172; questionType `fact_pursuit` 162, `motive_search` 160, `empathy_approach` 178; depth 1=130, 2=132, 3=114, 4=124; target `a`=237, `b`=263; selected `targetParty:both`=0; unique selected variant IDs=265.
- **Suggested next action**: CT-Main should decide whether TC-A3 spec should be updated to the implemented 84-template `soft/hard` model, or Codex-Dev should add `mid` tone and `evidence_present depth_3` support. Separately, Codex-Dev should treat `targetParty:both` as compatible with both `a` and `b` or retag those variants.
- **Files touched**: none, except this QA report under `tmp/qa-functional-results/`.

## P-3 Detail

- Initial baseline check: PASS, hard 0, warnings 157.
- Repeat count: 15 additional `npm run check:all` executions.
- Result stability: 15/15 exit 0; policy hard 0 every run; policy warnings 157 every run; sync issues 0 every run.
- Runtime range: 4.58s to 5.53s per run in this session.

## P-4 Static Matrix Detail

- Engine `QuestionType` union: `fact_pursuit`, `motive_search`, `empathy_approach`, `evidence_present`.
- Engine fallback pool total: 84 templates.
- `fact_pursuit`: depth_0..depth_3, soft/hard, 24 templates.
- `motive_search`: depth_0..depth_3, soft/hard, 24 templates.
- `empathy_approach`: depth_0..depth_3, soft/hard, 24 templates.
- `evidence_present`: depth_0..depth_2, soft/hard, 12 templates.
- Placeholder audit: no invalid placeholders; only `${name}` and `${subject}` found.
- Active scripted `judge_question`: 3 cases × 48 entries = 144 entries; 720 variants; every entry has 5 variants and at least one `targetParty:a` plus one `targetParty:b` variant.
