R5: WARN — 질문 subAction별 atom 선택 다양성
  상세: 엔진은 subAction별 primary/secondary/avoid tag를 기준으로 atom을 점수화한다(`src/engine/atomSelectionEngine.ts:63-147`, `src/types/claimV2.ts:197-237`)고, 프롬프트에도 `answerFocus`를 주입한다(`src/engine/blueprintPromptBuilderV2.ts:119`). 그러나 active 3종 등록은 `buildV3FallbackClaimPolicies`로 `publicClaim/privateKnowledge/suppressions`만 만들고 `claimAtoms`를 만들지 않는다(`src/data/claimPolicies/v3FallbackClaimPolicies.ts:47-86`).
  문제: 실제 active 사건은 `normalizeClaimPolicy`에서 legacy atom 합성으로 내려가며(`src/engine/atomSelectionEngine.ts:362-392`), 샘플 d-1/S2에서 `fact_pursuit`, `motive_search`, `empathy_approach`가 같은 legacy sentence를 공유했다. 질문 문체는 달라지지만 근거 atom 깊이는 충분히 분화되지 않는다.
  수정: 이번 QA 범위에서는 엔진 버그가 아니라 데이터 authoring/claimAtom 보강 이슈로 보고만 한다.
  severity: medium
