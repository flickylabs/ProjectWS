SurfaceName leak grep — before/after
Date: 2026-04-27

Before audit from HEAD:
- useActionDispatch pending evidence result used evDef.name.
- useActionDispatch actuallyDiscoverEvidence system message used ev.name.
- combo / unlock paths had direct def.name or evidence.name fallbacks.
- PC minigame / perk / discovery feedback paths could display evidence.name before deep investigation.
- e-4 truth name in policy data is "형 문자 스레드"; runtime allowed surface name is "발신자 미상 문자".

After grep:
Command:
  rg "형 문자 스레드" src/hooks src/components src/engine

Result:
  0 results

After runtime rule:
- getEvidenceDisplayName(def, state) returns def.name only when state.deepInvestigated is true.
- Otherwise UI/system paths prefer def.surfaceName, then fallback name/id.
