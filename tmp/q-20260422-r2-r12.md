R12: PASS — 사건 전환 시 이전 세션 오염 방지
  상세: `initializeCase`는 시작 시 `sessionStorage.removeItem(SAVE_KEY)`로 이전 persist를 제거한다(`src/store/useGameStore.ts:681`). 이후 agents/evidence/dialogue/discovery/combination lab을 재초기화한다(`src/store/useGameStore.ts:745-750`).
  문제: R11 보강으로 저장 항목이 늘었기 때문에 사건 전환 reset 누락 가능성이 생겼으나, pendingInterjectionV2/pendingEvidenceResult/disputeBoardAction/readiness/activePerks까지 초기화하도록 보강했다(`src/store/useGameStore.ts:700-728`).
  수정: reset 범위 보강 완료.
  severity: none
