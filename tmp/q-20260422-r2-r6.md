R6: PASS — 증거 단계형 조사/뷰어 정합성
  상세: 조사 성공 시 `investigatedActions`가 중복 없이 누적되고 `deepInvestigated`가 켜진다(`src/engine/evidenceEngine.ts:125-126`). 단계 질문 unlock은 조사 횟수 기반이다(`src/engine/evidenceEngine.ts:136-153`). PC 증거 뷰어는 현재 stage 이하의 가장 큰 `viewerDataByStage` key를 선택해 3회 이상 조사 시에도 마지막 단계에 clamp된다(`src/components/pc/evidence/PCEvidenceViewer.tsx:59-72`).
  문제: active 3종은 7개 증거 모두 `investigationStages` 0/1/2를 가진다. 다만 UI 특수 viewerDataByStage는 spouse-01 2개만 있고 family/friend는 기본 viewerData 중심이다.
  수정: 없음.
  severity: none
