R8: WARN — LieState 전이 단조성/종단성
  상세: 엔진은 S5에서 추가 전이를 막고(`src/engine/lieStateMachine.ts:28`), trust route/hard evidence/question fallback을 한 번에 한 단계만 처리한다(`src/engine/lieStateMachine.ts:30-58`). store의 `transitionLie`도 한 호출당 `attemptLieTransition` 1회만 적용한다(`src/store/slices/agentSlice.ts:111-122`). 질문 타입 트리거 매핑도 fact/motive/empathy별로 분리되어 있다(`src/hooks/useActionDispatch.ts:2157-2164`).
  문제: authored config 일부는 S4/S5 edge가 없다. 확인 결과 spouse a:d-2, family a:d-2, friend a:d-1/a:d-2가 S0->S3까지만 존재했다. 엔진 fallback으로 도달은 가능하지만, 해당 경로의 authored beat 품질은 낮다.
  수정: 엔진 단조성 문제는 없음. 데이터 전이 beat 보강 필요로 보고한다.
  severity: medium
