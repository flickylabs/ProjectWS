# API Proxy Migration Summary

Date: 2026-04-27

## Completed

- Added server-side OpenAI proxy handlers:
  - `/api/llm/dialogue`
  - `/api/llm/aftermath`
- Updated client LLM calls to use the proxy in production.
- Removed client-side OpenAI key env reads and client Authorization header construction.
- Updated result aftermath calls to use the aftermath proxy endpoint.
- Removed `VITE_OPENAI_API_KEY` fallback usage from eval routes and generation scripts.
- Updated README, HANDOFF, and GDD samples to server-only `OPENAI_API_KEY`.
- Redacted tmp debug key-shaped strings.

## Verification

- `npm run check:all`: PASS.
- `npm run build`: PASS.
- `npm run build:pc`: PASS.
- `npx tsc -b --force`: PASS.
- API/serverless type check: PASS.
- `dist-pc` / `dist` artifact grep for key-shaped strings and OpenAI env names: 0 matches.

## Remaining External Requirement

- Vercel must have a valid server-only `OPENAI_API_KEY` configured by the user.
- The local workspace currently has no usable OpenAI key value, so real upstream LLM calls were not completed locally; mock upstream proxy verification passed.
