# CT → Thread Q (Codex): 조합 시스템 대개편 직후 로직·엔진·상태 검증

> 발신: CT (Control Tower)
> 수신: Thread Q (Codex QA) — 로직 중심
> 일시: 2026-04-22
> 커밋: `d82a0e9` — feat: 조합 시스템 대개편 + 증거 단계 뷰어 + 증인 해금 게이팅 + 수많은 UX 버그 수정
> 브랜치: `main`
> 배포: Vercel 자동 빌드됨 (https://project-ws.vercel.app)

---

## 역할 분담

- **Thread Q (이 문서)**: 구조 정합성, 데이터 참조 무결성, 코드 로직, 상태 관리 — **논리에 집중**
- **Thread QW (별도)**: 한국어 품질, 대사 자연스러움, UX 텍스트 — **표현에 집중**

이번 세션에서 수정한 항목 중 **데이터/엔진 로직 측면**을 여기서 검증하고, **한국어·대사·UX 텍스트 측면**은 QW에서 검증합니다.

---

## 이번 커밋 요약 — 검증 대상

### A. 조합 시스템 플랫화 (데이터 구조 변경)
- **L-\* leads 완전 제거** (spouse/family/friend-01 3건)
- 모든 recipe.outputId가 `dc-*` 또는 쟁점/증거 효과로 직행
- `leadLines` 필드는 데이터에 남겨두지만 런타임 미참조

### B. spouse-01 스토리 정합성 재배치
- `d-2` = "개인 비자금 **2,000만원** 출금" (B측, 개인 계좌)
- `h-d3` = "공동 적금 **3,000만원** 해지 (위임장 조작)" (A측, 범죄)
- `e-5/e-6/e-7` description·investigationResults 금액 전수 정정
- `dc-3` 효과: `upgrade_dispute d-2` → **`unlock_dispute h-d3`** (스왑)
- `dc-4` 효과: `unlock_dispute h-d3` → **`upgrade_dispute d-2`** (스왑)
- `combine-5` inputs: `[stmt-a-protect, e-5]` → `[stmt-a-protect, e-7]`

### C. 증거 뷰어 단계별 확장 (신규 시스템)
- `EvidenceNode.viewerDataByStage?: Record<string, viewerData>` 타입 추가
- PCEvidenceViewer가 `state.investigatedActions.length` 기반으로 단계별 뷰어 선택
- `caseLoader.ts`에 `getOriginalViewerDataByStage()` 추가
- spouse-01 e-1(영수증), e-4(문자) 3단계 데이터 작성

### D. 증인 해금 게이팅 (신규 시스템)
- `ThirdParty.unlockedByDossier?: string[]` 타입 추가
- 스토어에 `unlockedWitnessIds: string[]` + `addUnlockedWitness` 추가
- `initializeCase`에서 기본 해금 증인 추림
- combinationLabSlice에서 dc-\* 달성 시 해당 증인 해금 + 시스템 메시지
- `canCallWitness`가 unlockedWitnessIds 체크
- `WitnessDetailSection` UI가 잠김 상태 표시 (`???`, 버튼 비활성)
- 3건 모든 증인(9개)에 `unlockedByDossier` 부여

### E. 버그 수정
- 끼어들기 v3 대사 로드 실패 (caseId 불일치) — `normalizeCaseId` 수정
- 증인 재잠금 — `partialize`에 `unlockedWitnessIds` 추가
- `timeline_lock` 시스템 메시지가 autoPin 되던 버그 — 직전 NPC 부정 발언에 autoPin
- `unlock_dispute`가 기존 hidden 쟁점을 실제 emerge 시키지 않던 버그 — `emergeDispute` 연결
- 감정 폭발 fallback 3인칭 서술이 당사자 말풍선에 들어가던 버그 — 시스템 메시지로 전환

### F. 조합 결과 UX
- `cleanOutputLabel`, `cleanOutputSummary`, `translateLeadLabel` 헬퍼로 영문 잔재 처리
- `discoveryText` 우선 사용
- 조합 슬롯이 `surfaceName` 표시 (내부 ID 노출 방지)
- `getNodeDisplayLabel` 헬퍼로 노드 친화적 이름 계산
- 대화 fuzzy 매칭 (화자 + 쟁점 컨텍스트)
- 동일 nodeId 중복 시 toast
- 모든 dc-\* 출력에 `judgeHint` + 조합 후 재판관 가이드 메시지 emit

### G. 증거 UI 보안
- `PCDisputeRibbon`에서 잠긴 증거 이름 `???` 마스킹

### H. 스크립트 텍스트 정정 (메타 누출 제거)
- spouse-01 e-2 GPS stage 2: "평일 저녁 식사시간 한정" 강조로 재작성
- spouse-01 e-3 통화기록 stage 2 + viewerData 통화시간 전부 5분 미만 재작성
- spouse-01 e-4 문자 stage 0/1 텍스트 재작성 + 3단계 뷰어
- 말풍선 코너 장식 `「」` 제거 (CSS)

---

## 수정 권한

- **데이터 파일** (JSON, TS 데이터): 직접 수정 가능 (ID 참조 오류, 구조 누락 등)
- **엔진 코드**: 직접 수정 가능 (로직 버그, 분기 오류 등)
- **UI 라우팅/상태 관리**: 직접 수정 가능
- **한국어 텍스트 품질**: 수정 금지 → Thread QW 관할 (FAIL 보고만)
- **수정 시 반드시 `npx tsc -b --force` 통과 확인**

---

## 검증 라운드 (25라운드 + 확장 가능)

> **최대한 꼼꼼하고 치밀하게 모두 돌릴 것.** 라운드 도중 파생 이슈 발견 시 임의 라운드를 추가해도 좋음. 각 라운드는 코드·데이터 **근거** 기반으로, 추측 금지.

### Phase A: 데이터 무결성 전수 (R1~R6)

**R1. 조합 시스템 플랫화 정합성 — 3건 전수**
- `combinationLab.nodes`에 `L-*` / `l-*` 계열 노드 0개 (spouse/family/friend-01)
- `combinationLab.outputs`에 `L-*` 출력 0개
- `combinationLab.recipes[].outputId`가 모두 실제 `outputs[].id`에 존재
- `combinationLab.recipes[].inputs[]`가 모두 실제 `nodes[].id`에 존재
- 모든 `dc-*` 출력에 도달 가능한 레시피 최소 1개 존재
- `v3Design.leadLines` 필드는 데이터에 존재해도 무방 (런타임 미참조) — 런타임에서 실제 참조하는지 확인 (grep 전체 src/)

**R2. spouse-01 금액/주체 스토리 정합성 (전수 grep)**
- `d-2`: name="개인 비자금 2,000만원 출금", quadrant="b_only", truthDescription이 B(이준호) 개인 자금 명시
- `h-d3`: name="공동 적금 3,000만원 해지 (위임장 조작)", quadrant="a_only", 위임장 조작 명시
- `e-5`: description + investigationResults 3키 모두 **2,000만원** 일관
- `e-6`: description + investigationResults 3키 모두 **3,000만원** 일관
- `e-7`: investigationResults 3키 모두 **3,000만원** + "박지연 대필" 명시
- `h-d4.truthDescription`: B 2천만 개인 / A 3천만 공동 적금 체계와 무모순
- 해당 JSON 전체 `grep -E "2[,.]?000만|3[,.]?000만|2천만|3천만"` 결과 전수 대조

**R3. dc-\* 효과 재매핑 검증 (3건)**
- spouse-01:
  - `dc-3.effects`: `[unlock_note→dc-3, unlock_dispute→h-d3]`
  - `dc-4.effects`: `[unlock_note→dc-4, upgrade_dispute→d-2 (weight=high, ambiguity=low)]`
  - `dc-1/dc-2/dc-5` 효과 일관성
- family-01: `dc-2/3/5`의 unlock_dispute 유지 + `dc-1/4`에 upgrade 추가됐는지
- friend-01: `dc-3/4/5`가 d-5/d-4/d-3 unlock_dispute 효과 포함 (이번 커밋에서 신규 추가)
- 3건 전 outputs에서 참조하는 dispute id가 실제 disputes에 존재

**R4. combinationLab 노드 참조 무결성 — 3건**
- recipes의 모든 inputs가 실제 노드 존재 (특히 spouse-01 `combine-5` inputs가 `[stmt-a-protect, e-7]`인지)
- statement 노드 `linkedDisputeIds`가 실제 dispute id 참조
- evidence/derived_evidence 노드가 `evidence[].id`와 일치
- witness_angle 노드가 socialGraph와 연결
- 사용되지 않는 노드(dead node) 있는지 전수 리포트

**R5. 증인 해금 데이터 — unlockedByDossier 전수**
- 3건 각 socialGraph(3명×3=9개) 모두 `unlockedByDossier` 필드 존재
- 지정된 dc-\* 값이 실제 outputs에 존재
- spouse-01: w-1=[dc-1], w-2=[dc-3,dc-4], w-3=[dc-3,dc-5]
- family-01: w-1=[dc-1], w-2=[dc-3], w-3=[dc-2,dc-5]
- friend-01: w-1=[dc-3], w-2=[dc-2], w-3=[dc-4]
- 지정된 dc-\*가 실제 도달 가능(R1과 교차) — 도달 불가 dc-\*가 게이트면 FAIL

**R6. R1~R5 발견 건 수정 + 재검증**
- `npx tsc -b --force` 통과
- `node tests/stage1-deep-audit.cjs` 있으면 실행

### Phase B: 엔진 로직 코드 리뷰 (R7~R14)

**R7. unlock_dispute 핸들러 — 기존 hidden 쟁점 emerge 연결**
- `combinationLabSlice.ts`의 `unlock_dispute` case:
  - `output.disputeNodes` 있으면 `upsertDerivedDisputes()` 호출 (신규 쟁점 추가)
  - `effect.unlockNodeId`(또는 targetId)가 기존 disputes에 존재하고 visibility='hidden'이면 `root.emergeDispute()` 호출
- spouse-01 dc-3 → h-d3 emerge 실제 동작
- spouse-01 dc-4 → d-2는 upgrade만 (unlock 아님)
- family-01 dc-2/3/5 → d-3/d-4/d-5 emerge 동작
- friend-01 dc-3/4/5 → d-5/d-4/d-3 emerge 동작

**R8. 증인 해금 게이팅 코드 경로 전수**
- `useGameStore.initializeCase`: `unlockedWitnessIds` 초기화 시 `unlockedByDossier`가 없거나 빈 증인만 포함
- `combinationLabSlice.runCombinationRecipe`: output.id가 `dc-*`로 시작하면 socialGraph 스캔 → `unlockedByDossier`에 output.id 포함된 증인을 `addUnlockedWitness` 호출 + 시스템 메시지 emit
- `witnessEngine.canCallWitness`: `unlockedWitnessIds` 인자 받아서 게이팅 체크
- `useActionDispatch` witness 분기에서 `state.unlockedWitnessIds` 전달 확인
- `partialize`에 `unlockedWitnessIds` 포함 여부 (세션 저장)
- `WitnessDetailSection` UI에서 `locked` 플래그 계산 로직

**R9. 증거 뷰어 단계별 선택 로직**
- `PCEvidenceViewer.tsx`:
  - `currentStage = state?.investigatedActions?.length ?? 0`
  - `getOriginalViewerDataByStage` 우선, 없으면 `evidence.viewerDataByStage` fallback
  - 단계 키 중 `<= currentStage` 중 최댓값 선택
  - 스테이지 매핑 없으면 기본 `viewerData` fallback
- `caseLoader.ts`의 `getOriginalViewerDataByStage` 구현 확인
- spouse-01 e-1: stage 0 → 편의점 2장, stage 1 → +올리브영(3장), stage 2+ → +다이소+교보문고(5장)
- spouse-01 e-4: stage 0 → 6개 애매 메시지, stage 1 → 11개(번호 노출), stage 2+ → 전체 13개
- **3건 모든 증거에 대해** viewerDataByStage 없어도 기본 뷰어가 정상 동작하는지

**R10. 끼어들기 v3 대사 로드 — 3건**
- `v3FallbackGameLoopData.ts.normalizeCaseId`: `caseData.caseId` 우선
- 3건 모두 v3 게임 루프 데이터가 `'spouse-01'`, `'family-01'`, `'friend-01'`로 등록되는지
- `getInterjectionEvent('spouse-01', 'spouse-01-interjection-b')` 정상 반환 — 3건 × 각 interjection id에 대해
- `PCDiscoveryOverlay.tsx`의 interjection 분기에서 `finalInterjectionText`가 v3 라인으로 채워지는지
- `scriptSlot.textId`가 `pendingEvent`에 실제 설정되는지 (gameEventTriggerEngine)

**R11. timeline_lock 대상 수정**
- `useGameStore.ts`의 `timeline_lock` case:
  - 시스템 메시지 `autoPin` 없음
  - `state.dialogueLog`에서 `entry.speaker === lockParty && relatedDisputes.includes(effect.disputeId) && !entry.autoPin`인 가장 최근 대화를 찾아 `autoPin: true` + `behaviorHint: '진술 시점 고정'` 설정
- 즐겨찾기가 시스템 메시지가 아닌 실제 NPC 부정 발언에 꽂히는지
- 매칭되는 NPC 대화가 없으면 시스템 메시지만 남는지(fallback)

**R12. 감정 폭발 fallback 말풍선 분기**
- `PCDiscoveryOverlay.tsx`의 emotional_burst 분기:
  - `outburstLine` 있으면 `speaker: pendingEvent.party` 말풍선
  - 없으면 `speaker: 'system'` 메시지 + `pendingEvent.description`
- `emitOutburst()` 헬퍼가 `handlePress`/`handleCalm` 양쪽에서 재사용
- 오버레이 프리뷰도 `outburstLine`이면 quote, 아니면 narration 스타일

**R13. 조합 결과 표시·라벨 파이프라인**
- `cleanOutputLabel`: `L-1 Timeline Lead` → `동선 단서` (translateLeadLabel)
- `cleanOutputSummary`: `Timeline 리드` → `동선 단서` (label과 중복 시 빈 문자열)
- `getNodeDisplayLabel`: evidence → surfaceName 우선, statement → quote만 추출
- `body: matchingRecipe.discoveryText || cleanOutputSummary(...) || judgeHint || ''`
- 시스템 메시지·popup·animation overlay 3경로 모두 동일 로직 적용
- `pc:combination-success` 이벤트 detail의 inputs/outputLabel/outputSummary 모두 cleaned

**R14. R7~R13 발견 건 수정 + 재검증**

### Phase C: 리소스·상태·세션 (R15~R18)

**R15. partialize 화이트리스트 전수 점검**
- `useGameStore.ts`의 `partialize` 블록에서:
  - `unlockedWitnessIds`: 포함 확인 (이번 커밋 신규)
  - `calledWitnesses`: 유지
  - `combinationLabRuntime`: 해금 노드/발견 메모 등 포함
  - 새로고침(F5) 시 위 상태가 전부 복원되는지 수동 테스트
- 초기 해금 증인 목록 ≠ 실제 해금 증인일 때(조합 이후) 새로고침 시 조합으로 해금한 증인이 잠기지 않는지

**R16. 리소스 경제 회귀 검증**
- `INITIAL_RESOURCES = { investigationTokens: 20, skillPoints: 10, courtControl: 5 }`
- 조합 실행 시 skillPoints 1 소비
- 증거 조사 시 investigationTokens 1 소비
- 증인 소환 시 investigationTokens 1 소비, 차단 시 환불
- 이번 커밋 수정 중 resource spend/gain 경로에 영향 없음 확인

**R17. Phase 전환 엣지 케이스**
- 16턴 초과 → 강제 Mediation (incrementTurn 내 분기)
- 조합으로 hidden dispute emerge 직후 phase 전환 시도 — 상태 무결 확인
- Phase.Interrogation → Mediation 진입 시 조합 슬롯 / 해금 증인 상태 유지
- Phase.Verdict 진입 시 이번 커밋 수정들이 점수 계산에 영향 없는지

**R18. 교차 사건 세션 전환**
- spouse-01 플레이 → 종료 → family-01 시작 시:
  - `unlockedWitnessIds`가 family-01 초기값으로 재계산되는지
  - `combinationLabRuntime`이 family-01 config로 재초기화되는지
  - 이전 사건의 dc-\* / 조합 흔적이 남지 않는지
- 같은 사건 재플레이 시 조합 해금 상태가 올바르게 초기화되는지

### Phase D: 런타임 플레이스루 (R19~R22)

**R19. spouse-01 End-to-End 플레이스루**
- Phase 0~Result 완주
- 새 금액 체계 대사·증거에 반영된 채 진행
- e-1+e-2 → dc-1 + w-1 해금 메시지
- e-5+e-3 → dc-4, d-2 강화
- e-6+e-7 → dc-3 + **h-d3 쟁점 리본 등장** + w-2/w-3 해금
- 증인 패널에서 해금된 증인만 소환 가능
- 단계별 뷰어 동작 (e-1, e-4)
- TypeError / console.error 0건

**R20. family-01 End-to-End 플레이스루**
- L-\* 제거로 인한 회귀 없음
- 모든 dc-\* 도달
- dc-2/3/5 → d-3/4/5 emerge
- 증인 3명 해금 체인

**R21. friend-01 End-to-End 플레이스루**
- L-\* 제거로 인한 회귀 없음
- dc-3/4/5 → 이번에 신규 추가한 unlock_dispute 효과로 h-d 쟁점 emerge
- 증인 3명 해금 체인

**R22. 엣지 케이스 집중**
- 새로고침(F5) — 조합 직후 / 증인 해금 직후 / 단계별 조사 후
- 끼어들기 허용 → v3 1인칭 대사 노출 (3건 × 파티)
- 감정 폭발 v3 없는 경로 → 시스템 메시지 (말풍선 아님)
- 모순 추궁 → timeline_lock → 즐겨찾기 핀이 시스템 메시지가 아닌 NPC 발언에 찍히는지
- 단계별 뷰어 — 조사 0/1/2회 상태에서 각각 열람

### Phase E: 빌드·품질 (R23~R25)

**R23. `npx tsc -b --force` 통과**
- 0 error, 0 warning (새 경고 포함)
- 이번 커밋의 신규 타입 (`viewerDataByStage`, `unlockedByDossier`, `unlockedWitnessIds`) 모두 해석 가능

**R24. `npm run build` 통과**
- 프로덕션 번들 생성 + size 비정상 증가 없음
- 빌드 경고 최소화

**R25. 최종 보고서 작성 → `tmp/thread-q-20260422-report.md`**
- Phase A~E 결과 요약
- severity별 카운트 + 블로커 목록
- 수정 파일 목록 (커밋 hash 변경되면 기록)
- **QW로 위임한 항목** (한국어 품질)
- 최종 판정: PASS / FAIL / CONDITIONAL

### 추가 라운드 (필요 시 R26+)

다음 징후 발견 시 라운드 추가하고 보고:
- 신규 엔진 버그 → R26 (버그 재현 시나리오 + 수정)
- 파생 리그레션 → R27 (영향 범위 재검증)
- 성능 이슈 (초기화 지연, 대량 노드 처리) → R28
- 사건 간 격리 위반 → R29
- sessionStorage 스키마 마이그레이션 필요 → R30

### 라운드별 산출물

각 라운드 종료 시 `tmp/q-20260422-r{N}.md` 파일에 결과 기록.

---

## 먼저 읽을 파일 (순서대로)

1. `CLAUDE.md` — 전체 구조
2. `src/data/cases/generated/spouse-01.json` — 조합/쟁점/증거/증인 데이터
3. `src/data/cases/generated/family-01.json`
4. `src/data/cases/generated/friend-01.json`
5. `src/types/case.ts` — EvidenceNode.viewerDataByStage 필드
6. `src/types/character.ts` — ThirdParty.unlockedByDossier 필드
7. `src/store/slices/combinationLabSlice.ts` — unlock_dispute emerge 연결 + 증인 해금 훅
8. `src/store/useGameStore.ts` — unlockedWitnessIds 상태 + timeline_lock 수정 + partialize
9. `src/engine/witnessEngine.ts` — canCallWitness 시그니처 변경
10. `src/hooks/useActionDispatch.ts` — canCallWitness 호출부에 unlockedWitnessIds 전달
11. `src/components/pc/evidence/PCEvidenceViewer.tsx` — 단계별 뷰어 선택 로직
12. `src/data/cases/caseLoader.ts` — getOriginalViewerDataByStage
13. `src/components/pc/layout/PCInteractionPanel.tsx` — WitnessDetailSection 잠금 UI
14. `src/components/pc/layout/PCDiscoveryOverlay.tsx` — 끼어들기 v3 로드 + 감정 폭발 분기
15. `src/components/pc/layout/PCDisputeRibbon.tsx` — 잠긴 증거 마스킹
16. `src/components/pc/panels/PCRightPanel.tsx` — 조합 UI + cleanOutputLabel/judgeHint emit
17. `src/data/claimPolicies/v3FallbackGameLoopData.ts` — normalizeCaseId 수정

---

## 출력 형식

```
R{번호}: {PASS/FAIL/WARN} — {검증 항목}
  상세: {확인한 내용, 코드 위치/라인 번호 포함}
  문제: {발견한 문제 또는 "없음"}
  수정: {직접 수정 내용 또는 "CT 위임"}
  severity: {critical/high/medium/low/none}
```

최종 요약에서:
1. severity별 카운트
2. 전체 PASS/FAIL/WARN 비율
3. **수정한 파일 목록** (commit hash 변경 여부)
4. **QW로 위임한 항목 목록** (한국어 품질 관련)

---

## PASS 기준

| 등급 | 조건 |
|------|------|
| **PASS** | Phase A~D 전체 PASS |
| **CONDITIONAL** | Phase B/C에서 경미 WARN만 (기능 영향 없음) |
| **FAIL** | Phase A FAIL 1건 이상, 또는 Phase B/C 블로커 1건 이상 |

---

## 주의사항

- **한국어 대사·텍스트 품질은 QW가 담당 — 로직 검증에만 집중**
- 추측이 아닌 **코드/데이터 근거(파일 경로, 라인 번호, JSON 경로)** 기반 보고
- 수정 후 반드시 `npx tsc -b --force` 통과 확인
- 발견한 문제가 **L-\*, leadLines, viewerDataByStage, unlockedByDossier, unlockedWitnessIds** 같은 이번 커밋의 신규 필드/개념과 관련되면 특별히 상세하게 보고
- 기존 spouse-01 금액 체계(3천만 vs 2천만)가 **스왑된 것이 의도적**임 — 금액 체계가 과거 버전과 다르다고 FAIL 처리하지 말 것
