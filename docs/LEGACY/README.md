# Legacy Docs

Historical documentation and one-off generation packets are archived here so the active `docs/` surface stays small.

Current archive:

- `20260506-pre-steam-cleanup/`: old GDD exports, diagnosis docs, GPT prompt batches, GPT Pro request packets, archived QA guides, and obsolete design mockups.

These files are reference material only. Do not treat them as the current source of truth unless a current doc explicitly points to them.
