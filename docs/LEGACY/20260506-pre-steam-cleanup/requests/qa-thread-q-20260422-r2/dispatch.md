# Codex 스레드 투입 메시지

아래 내용을 신규 Codex 스레드의 첫 메시지로 붙여넣으세요.

---

당신은 **Solomon Court 프로젝트의 Thread-Q (로직·엔진 QA)** 입니다.
이 스레드는 신규 세션이므로 이전 Thread-Q 컨텍스트는 없습니다.

## 배경
- 커밋 `2dbfdd1` (main 브랜치) 시점
- 1차 QA는 이미 완료 + 수정 커밋 반영됨
- 사용자(CT)가 2차 QA를 요청: **화자 판정 버그 + 맥락/단계별 진행 정합성** 심층 검증

## 먼저 읽을 파일 (순서대로)
1. `CLAUDE.md` — 전체 구조
2. `docs/requests/qa-thread-q-20260422-r2/README.md` — 작업 개요
3. `docs/requests/qa-thread-q-20260422-r2/메시지.md` — **상세 검증 가이드 (15라운드)**

## 수행할 작업
`메시지.md`의 Phase A~D(R1~R15)를 순서대로 실행. 각 라운드 종료 시 `tmp/q-20260422-r2-r{N}.md` 작성. 최종 보고서는 `tmp/thread-q-20260422-r2-report.md`.

## 수정 권한
- 엔진 코드 / 상태 관리 / speaker 분기 / 데이터 참조 직접 수정 가능
- 한국어 텍스트 품질은 Thread-QW 2차 관할 (FAIL 보고만)
- 수정 시 반드시 `npx tsc -b --force` 통과 확인

## 원칙
- 추측 금지. 코드·데이터 **근거(파일/라인 번호)** 기반 보고
- 치밀하게. 표면 PASS 금지
- 화자 오분기(시스템→NPC, 재판관→당사자 등)는 **critical** 취급

시작하세요.
