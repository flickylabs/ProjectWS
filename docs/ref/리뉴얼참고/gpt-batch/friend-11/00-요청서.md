# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "friend-11",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "friend11:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "friend-11",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `friend-11-v2-atoms.json`
요청 A 결과물

### 파일 2: `friend-11-tells-beats.json`
```json
{
  "caseId": "friend-11",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `friend-11-v3-game-loop-data.json`
```json
{
  "caseId": "friend-11",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### friend-11

**당사자**:
- a: **최민우** (29세, 이커머스 CS 매니저, archetype: victim_cosplay)
- b: **배준석** (30세, 번역 프리랜서, archetype: cold_logic)

**쟁점 5개**:
- d-1: 준석의 일방적 접근 차단과 공유 프로필 정리 (truth=true, weight=high)
- d-2: 민우의 개인정보 무단 재사용과 관리자 메일 변경 (truth=true, weight=high)
- d-3: 계정 탈취의 출발점은 준석의 선공 독점이었는가 (truth=false, weight=high)
- d-4: 작년 '일회성 본인확인 후 삭제' 규칙의 파기 (truth=true, weight=medium)
- d-5: 사라진 시즌패스 아이템과 저장 파일은 준석이 가져간 것인가 (truth=false, weight=high)

**증거 6개**:
- e-1: 민우가 공통 친구 방에 올린 로그인 차단 캡처 [soft] **함정** → d-1,d-3,d-5
- e-2: 작년 일회성 본인확인 합의 카톡과 결제 메모 [hard] → d-2,d-4
- e-3: 구독 서비스 보안 로그와 관리자 메일 변경 기록 [hard] → d-2,d-3
- e-4: 민우 휴대폰 갤러리의 청구지 캡처와 생년월일 메모 [hard] → d-2,d-3
- e-5: 준석의 계정 복구 신청서와 접근권 회수 로그 [hard] → d-1,d-4
- e-6: 아이템 이전·환불 내역과 프로필 복원 보고서 [hard] → d-1,d-5

**lieConfig**:
- a (3): d-2(LT-1 L3 self_protection), d-3(LT-4 L2 face_saving), d-4(LT-5 L1 shame_avoidance)
- b (3): d-1(LT-2 L2 self_protection), d-5(LT-6 L1 self_protection), d-4(LT-7 L1 relationship_maintenance)

**화법 스타일** (Tell 설계 참고):
- 최민우: 먼저 자신이 얼마나 갑자기 계정에서 튕겼는지와 사라진 프로필, 잃은 게임 시간을 길게 말하며 피해자 자리를 선점한다. 불리해지면 관리자 권한 문제보다 '내 저장파일이 날아갔다'는 결과를 반복해 초점을 이동시킨다.
- 배준석: 날짜와 보안 알림 시간을 정확히 짚으며 말하고, 감정 표현보다 어떤 기기에서 어떤 변경이 있었는지를 먼저 정리한다. 다만 몰리면 설명보다 로그 화면을 오래 보여 주며 자신의 일방적 차단이 준 충격을 축소하려 한다.

**Tell 제안** (archetype 기반):
- 최민우(victim_cosplay): 희생 나열(enumeration), 목 잠김(self_reference), 되물음(question_end)
- 배준석(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)

**원본 verbalTells**:
- 최민우:
  - loss_preload: "불리한 질문이 나오면 '내 레벨이랑 시즌패스가 먼저 날아갔어'라고 피해 결과를 먼저 깔아 행위 책임을 뒤로 미룬다."
  - motive_flip: "개인정보 재사용을 묻으면 '그걸 쓴 게 아니라 계정을 살리려 한 거야'라고 의도로 수위를 바꾼다."
  - wounded_breath: "억울해질수록 짧게 한숨을 내쉰 뒤 같은 손해 항목을 두세 번 반복한다."
- 배준석:
  - log_recital: "곤란한 질문이 나오면 '오전 9시 14분, 9시 19분'처럼 시각을 길게 읊어 감정적 책임을 뒤로 미룬다."
  - label_split: "'차단'이 아니라 '복구 후 접근 정리'였다고 말하며 같은 행동의 이름을 바꿔 수위를 낮춘다."
  - clipped_breath: "민우의 무단 인증을 떠올릴 때 숨을 짧게 끊고 문장을 더 딱딱하게 자른다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
