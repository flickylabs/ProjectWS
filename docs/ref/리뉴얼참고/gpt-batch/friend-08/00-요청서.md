# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "friend-08",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "friend08:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "friend-08",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `friend-08-v2-atoms.json`
요청 A 결과물

### 파일 2: `friend-08-tells-beats.json`
```json
{
  "caseId": "friend-08",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `friend-08-v3-game-loop-data.json`
```json
{
  "caseId": "friend-08",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### friend-08

**당사자**:
- a: **조은비** (24세, 시각디자인과 4학년, archetype: victim_cosplay)
- b: **김준혁** (25세, 컴퓨터공학과 복학생, archetype: cold_logic)

**쟁점 5개**:
- d-1: 은비의 단독 성과처럼 보이게 한 게시물과 포트폴리오 문구 (truth=true, weight=high)
- d-2: 준혁의 마감 직전 자료 누락과 리허설 불참 (truth=true, weight=high)
- d-3: 공모전과 심사위원은 이 프로젝트를 은비 개인 작품으로 인식했는가 (truth=false, weight=high)
- d-4: 기여도 기록과 제출 규칙을 둘 다 방치한 공동 책임 (truth=true, weight=medium)
- d-5: 준혁의 기여는 아이디어 한 줄뿐이었다는 주장 (truth=false, weight=high)

**증거 6개**:
- e-1: 시상 직후 올라온 카드뉴스와 포트폴리오 캡처 [soft] **함정** → d-1,d-3
- e-2: 공모전 제출 form export와 수정 이력 [hard] → d-3,d-4
- e-3: 깃 저장소와 공유드라이브 revision log [hard] → d-2,d-5
- e-4: 팀 회의록과 리허설 출석표 [hard] → d-2,d-4
- e-5: 심사위원 회신 메일과 상장 원본 [hard] → d-3,d-5
- e-6: 은비 포트폴리오 캡션 수정 기록과 동아리 게시물 edit history [hard] → d-1

**lieConfig**:
- a (3): d-1(LT-2 L2 career_preservation), d-5(LT-4 L2 face_saving), d-4(LT-6 L1 relationship_maintenance)
- b (3): d-2(LT-2 L2 self_protection), d-3(LT-5 L1 shame_avoidance), d-4(LT-3 L1 face_saving)

**화법 스타일** (Tell 설계 참고):
- 조은비: 자신이 밤새 수습한 일과 발표 때 감당한 압박을 먼저 길게 설명하며 공로를 정당화한다. 불리해지면 고의가 아니라 대표자 역할의 관성 때문이었다고 말하며 표현 수위를 낮춘다.
- 김준혁: 커밋 시간, 설문 버전, 회의 날짜처럼 기록 가능한 요소를 중심으로 말한다. 하지만 인정받지 못했다는 감정이 커지면 숫자와 로그를 들이대며 상대의 의도까지 확정해 버린다.

**Tell 제안** (archetype 기반):
- 조은비(victim_cosplay): 희생 나열(enumeration), 목 잠김(self_reference), 되물음(question_end)
- 김준혁(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)

**원본 verbalTells**:
- 조은비:
  - rescue_stack: "질문을 받으면 본론보다 자신이 고친 화면, 다시 뽑은 출력물, 발표 직전 수습한 일을 길게 나열한다."
  - caption_minimizing: "문제가 된 문구를 지적받으면 '캡션 한 줄이었어'라고 표현의 파급력을 축소한다."
  - tired_laugh: "억울할 때 짧게 웃은 뒤 '그날 누가 남아 있었는지 생각해 봐'라고 피로를 근거로 내세운다."
- 김준혁:
  - log_drop: "불리한 지점이 나오면 로그 캡처를 한꺼번에 보여 주며 개별 질문을 기술 기록 더미 속에 묻는다."
  - binary_credit: "'내 거였어, 아니었어'처럼 공로를 이분법으로 나눠 중간 지대를 지우려 한다."
  - dry_pause: "감정이 올라오면 대답 전 짧게 침묵하고, 이후 더 딱딱한 톤으로 사실만 열거한다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
