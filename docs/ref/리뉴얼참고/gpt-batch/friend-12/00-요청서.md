# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "friend-12",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "friend12:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "friend-12",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `friend-12-v2-atoms.json`
요청 A 결과물

### 파일 2: `friend-12-tells-beats.json`
```json
{
  "caseId": "friend-12",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `friend-12-v3-game-loop-data.json`
```json
{
  "caseId": "friend-12",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### friend-12

**당사자**:
- a: **윤수빈** (27세, 카피라이터 겸 인디 보컬, archetype: confrontational)
- b: **백재윤** (29세, 숏폼 영상 편집자 겸 비트메이커, archetype: cold_logic)

**쟁점 5개**:
- d-1: 수빈의 '내 신곡/내 프로젝트' 단독 홍보 (truth=true, weight=high)
- d-2: 재윤의 포트폴리오 릴과 안내 메일에서 수빈 누락 (truth=true, weight=high)
- d-3: 최종작 '새벽역'은 사실상 한 사람의 단독 창작물인가 (truth=false, weight=high)
- d-4: 최종 크레딧 시트와 역할 정의를 끝까지 비워 둔 공동 책임 (truth=true, weight=medium)
- d-5: 재윤의 one-name 화면과 포스터 크롭 재활용 (truth=true, weight=high)

**증거 6개**:
- e-1: 스트리밍 카드·포스터·릴 썸네일 캡처 모음 [soft] **함정** → d-1,d-2,d-5
- e-2: 공유 노션 기획안과 크레딧 메모 [hard] → d-3,d-4
- e-3: DAW 세션 파일과 가사·멜로디 수정 이력 [hard] → d-3
- e-4: 편집 프로젝트 파일과 촬영 큐시트 [hard] → d-2,d-3
- e-5: 쇼케이스 제출 폼과 음원유통 메타데이터 export [hard] → d-3,d-5
- e-6: 인스타·릴스 edit history와 크레딧 조율 단톡 [hard] → d-1,d-2,d-4,d-5

**lieConfig**:
- a (3): d-1(LT-2 L2 career_preservation), d-3(LT-4 L2 face_saving), d-4(LT-6 L1 relationship_maintenance)
- b (3): d-2(LT-2 L2 self_protection), d-3(LT-3 L2 face_saving), d-5(LT-4 L1 career_preservation)

**화법 스타일** (Tell 설계 참고):
- 윤수빈: 결과물이 세상에 어떻게 소개됐는지를 먼저 말하며, 자신이 쓴 문장과 부른 멜로디를 중심으로 창작의 핵심을 정의한다. 불리해지면 기술적 기여는 수고였을 뿐 작품의 정체성은 자신에게 있었다고 몰아간다.
- 백재윤: 프로젝트 파일과 버전 기록, 큐시트 순서를 차례로 보여 주며 누가 실제로 무엇을 만들었는지를 로그로 입증하려 한다. 하지만 몰리면 크레딧 문제를 '표시 형식' 문제로 줄여 자신의 과장된 자기 홍보를 가볍게 다루려 한다.

**Tell 제안** (archetype 기반):
- 윤수빈(confrontational): 증거 흔들기(echo_repeat), 동기 단정(self_reference), 선택적 인용(echo_repeat)
- 백재윤(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)

**원본 verbalTells**:
- 윤수빈:
  - identity_push: "불리한 질문이 나오면 '노래의 정체성은 내 목소리였어'라고 작품의 핵심을 자신에게 먼저 묶는다."
  - caption_minimizing: "문제된 단독 표현을 지적받으면 '캡션 한 줄이었어'라며 파급력을 축소한다."
  - tight_cutoff: "감정이 올라오면 상대 설명을 중간에 끊고 예스·노 식의 짧은 질문으로 몰아간다."
- 백재윤:
  - log_drop: "불리한 지점이 나오면 커밋과 프로젝트 타임라인을 한꺼번에 내밀어 개별 질문을 기록 더미에 묻는다."
  - format_split: "'표시'와 '소유'는 다르다며 공개 화면의 인상을 과소평가한다."
  - dry_pause: "감정이 올라오면 짧게 침묵한 뒤 더 무미건조한 톤으로 사실만 열거한다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
