# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "neighbor-11",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "neighbor11:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "neighbor-11",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `neighbor-11-v2-atoms.json`
요청 A 결과물

### 파일 2: `neighbor-11-tells-beats.json`
```json
{
  "caseId": "neighbor-11",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `neighbor-11-v3-game-loop-data.json`
```json
{
  "caseId": "neighbor-11",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### neighbor-11

**당사자**:
- a: **유성재** (35세, 5층 제품촬영 스튜디오 대표, archetype: cold_logic)
- b: **김라은** (41세, 4층 수제디저트 카페 운영자, archetype: victim_cosplay)

**쟁점 5개**:
- d-1: 성재의 서비스 태그 복제와 창고 출입 (truth=true, weight=high)
- d-2: 라은의 편집 영상·재고손실 연출 (truth=true, weight=high)
- d-3: 상층-하층 갈등 자체가 공모였는가 (truth=true, weight=high)
- d-4: 건물주가 원래 보상안을 전혀 검토하지 않았는가 (truth=false, weight=medium)
- d-5: 관리대행사 확인 전 공개 청구와 압박 (truth=true, weight=medium)

**증거 6개**:
- e-1: 라은이 돌린 31초 창고 침입 클립 [soft] **함정** → d-2,d-5
- e-2: 서비스도어 출입로그와 성재 휴대폰 NFC 복제앱 기록 [hard] → d-1
- e-3: 라은의 재고손실 사진과 직원 진술 초안 [hard] → d-2
- e-4: 건물주 측 보수·임대료 감면안 초안 이메일 [hard] → d-4
- e-5: 성재-라은 비밀 메신저 대화 [hard] → d-3,d-5
- e-6: 보상요구서 문서 메타데이터와 포렌식 비교표 [hard] → d-3,d-4

**lieConfig**:
- a (3): d-1(LT-2 L3 self_protection), d-3(LT-1 L3 self_protection), d-5(LT-7 L1 shame_avoidance)
- b (3): d-2(LT-2 L3 revenge), d-3(LT-1 L3 self_protection), d-5(LT-4 L1 face_saving)

**화법 스타일** (Tell 설계 참고):
- 유성재: 질문을 받으면 출입기록, 장비 상태, 손실 시간을 먼저 말한다. 불리해지면 실제 하자와 연출된 장면을 한 문장에 섞어 자신이 한 행위를 정당한 대응처럼 보이게 만든다.
- 김라은: 먼저 재고가 흐트러진 사진과 냄새, 매출 손실을 길게 설명하며 피해자 자리를 선점한다. 불리한 질문이 나오면 '실제 피해도 있었잖아요'라며 연출과 현실 피해를 섞는다.

**Tell 제안** (archetype 기반):
- 유성재(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)
- 김라은(victim_cosplay): 희생 나열(enumeration), 목 잠김(self_reference), 되물음(question_end)

**원본 verbalTells**:
- 유성재:
  - log_wall: "무단 침입 여부를 묻는 질문에 출입시각, 카드번호, 보안앱 로그를 길게 읊어 핵심 허락 관계를 흐린다."
  - cause_merge: "실제 누수 피해와 자신이 만든 연출 장면을 한 덩어리로 묶어 '원래 피해가 있었으니 이건 과장이 아니다'는 식으로 말한다."
  - dry_tone: "라은과의 사전 협의를 묻는 말이 나오면 감정을 완전히 빼고 기술 설명으로만 버틴다."
- 김라은:
  - damage_stack: "흐트러진 박스, 젖은 종이, 냄새, 손님 취소를 한꺼번에 쌓아 실제보다 큰 피해처럼 만든다."
  - moral_jump: "증거 조작을 추궁받으면 곧바로 '건물주가 계속 무시했으니 그 정도는 할 수밖에 없었다'는 식으로 도덕 판단을 바꿔 버린다."
  - shared_pain_cover: "성재와의 공모 여부를 묻는 질문이 오면 '우리 둘 다 당한 사람'이라는 표현으로 공모 사실을 공동 피해 언어로 덮는다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
