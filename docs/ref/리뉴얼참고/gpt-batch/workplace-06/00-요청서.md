# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "workplace-06",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "workplace06:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "workplace-06",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `workplace-06-v2-atoms.json`
요청 A 결과물

### 파일 2: `workplace-06-tells-beats.json`
```json
{
  "caseId": "workplace-06",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `workplace-06-v3-game-loop-data.json`
```json
{
  "caseId": "workplace-06",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### workplace-06

**당사자**:
- a: **박준호** (42세, 통합수익운영팀 팀장, archetype: avoidant)
- b: **이서린** (34세, 고객성공 선임, archetype: confrontational)

**쟁점 5개**:
- d-1: 준호의 계정 재배정과 대시보드 직접 수정 (truth=true, weight=high)
- d-2: 서린의 레거시 관리자 권한 사용과 라우팅 규칙 변경 (truth=true, weight=high)
- d-3: 통합 공지가 준호 또는 서린 한쪽에 단독 통제권을 준 것이었는가 (truth=false, weight=high)
- d-4: 준호의 선행 평가 메모와 경계 위반 낙인 (truth=true, weight=medium)
- d-5: 고객 지연 신호 후 양측의 독단적 핫픽스 (truth=true, weight=medium)

**증거 6개**:
- e-1: 팀즈 통합공지 캡처와 잘린 역할표 [soft] **함정** → d-1,d-3
- e-2: 레거시 관리자 권한 감사 로그와 라우팅 규칙 변경 내역 [hard] → d-2,d-5
- e-3: 공식 통합 RACI 문서와 임시권한 해설 메모 [hard] → d-3
- e-4: 통합 큐 재배정 기록과 대시보드 편집 이력 [hard] → d-1,d-5
- e-5: HR 캘리브레이션 초안과 매니저 코멘트 [hard] → d-4
- e-6: 부서통합 PM 회의록과 에스컬레이션 체크리스트 [hard] → d-3,d-5

**lieConfig**:
- a (3): d-1(LT-2 L2 face_saving), d-4(LT-6 L2 career_preservation), d-5(LT-3 L1 self_protection)
- b (3): d-2(LT-1 L2 self_protection), d-3(LT-4 L1 face_saving), d-5(LT-7 L1 shame_avoidance)

**화법 스타일** (Tell 설계 참고):
- 박준호: 갈등이 생기면 먼저 통합 초기의 혼란과 전체 KPI를 말하며 개별 행동의 순서를 뒤로 미룬다. 몰리면 '일단 멈추지 않게 하려 했다'는 표현으로 독단적 판단을 운영상 필요처럼 설명한다.
- 이서린: 상대가 남긴 문장을 그대로 읽어주며 약속과 권한의 경계를 분명히 하려 한다. 그러나 몰리면 자신이 한 수정은 '막기 위한 임시 조치'였다고 공격적으로 축소한다.

**Tell 제안** (archetype 기반):
- 박준호(avoidant): 숫자 정밀화(number_first), 되묻기(question_end), 동선 나열(enumeration)
- 이서린(confrontational): 증거 흔들기(echo_repeat), 동기 단정(self_reference), 선택적 인용(echo_repeat)

**원본 verbalTells**:
- 박준호:
  - chaos_cover: "구체적 월권 여부를 묻는 질문에도 '그날은 시스템이 다 흔들렸다'며 혼란 자체를 방패로 쓴다."
  - temporary_language: "'잠깐 손댄 거다', '임시로 돌린 거다'처럼 조치의 강도를 계속 축소한다."
  - order_skip: "누가 먼저 승인 절차를 건너뛰었는지 대신 결과가 나빴다는 이야기로 점프한다."
- 이서린:
  - quote_and_point: "상대가 남긴 공지 문장을 그대로 되읽으며 자기 월권 사실을 잠시 가린다."
  - permission_narrowing: "'수정'이 아니라 '복구'였다고 명칭을 바꾸며 권한 남용 범위를 줄인다."
  - sharp_overlap: "억울함이 커지면 상대 말을 끊고 자신의 업무영역 정의를 반복해 대화가 점점 더 충돌적으로 변한다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
