# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "workplace-07",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "workplace07:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "workplace-07",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `workplace-07-v2-atoms.json`
요청 A 결과물

### 파일 2: `workplace-07-tells-beats.json`
```json
{
  "caseId": "workplace-07",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `workplace-07-v3-game-loop-data.json`
```json
{
  "caseId": "workplace-07",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### workplace-07

**당사자**:
- a: **장태욱** (40세, 품질보증팀장, archetype: cold_logic)
- b: **한세린** (32세, 품질감사 대리, archetype: victim_cosplay)

**쟁점 5개**:
- d-1: 태욱의 익명 제보자 색출과 선행 낙인 (truth=true, weight=high)
- d-2: 세린의 익명 게시글과 편집 스크린샷 유포 (truth=true, weight=high)
- d-3: 게시글의 핵심 스크린샷은 태욱의 현 시점 증거인멸 지시를 그대로 담고 있는가 (truth=false, weight=high)
- d-4: 태욱의 과거 제보 축소와 평가 개입 (truth=true, weight=high)
- d-5: 과거 미종결 사건을 현재 명예전으로 재활용한 쌍방 가담 (truth=true, weight=medium)

**증거 6개**:
- e-1: 익명 게시글 보존본과 태욱의 팀 미팅 메모 [hard] → d-1,d-5
- e-2: 세린이 제출한 메신저 스크린샷 묶음 [soft] **함정** → d-2,d-3
- e-3: 메신저 원본 export와 메시지 해시 비교표 [hard] → d-3
- e-4: 2년 전 결함등급 하향 회의록과 후속 메일 [hard] → d-4,d-5
- e-5: 윤리경영 조사 접수기록과 태욱의 인사 개입 로그 [hard] → d-1,d-4
- e-6: 디지털포렌식 레이어 분석 메모와 편집PC 로그 [hard] → d-3,d-5

**lieConfig**:
- a (3): d-1(LT-6 L2 career_preservation), d-4(LT-3 L2 face_saving), d-5(LT-4 L1 self_protection)
- b (3): d-2(LT-1 L2 revenge), d-3(LT-4 L3 career_preservation), d-5(LT-7 L2 shame_avoidance)

**화법 스타일** (Tell 설계 참고):
- 장태욱: 질문을 받으면 메시지 원본, 절차, 등급 기준부터 먼저 설명하며 감정을 최대한 배제한다. 하지만 몰리면 현재 건과 과거 건을 인위적으로 떼어 놓으며 자신이 남긴 압박성 코멘트의 맥락을 축소한다.
- 한세린: 처음에는 캡처와 시간표를 조용히 꺼내지만, 곧바로 자신이 과거에도 눌렸던 제보자라는 프레임으로 대화를 끌고 간다. 몰리면 현재 증거의 문제를 과거 억압의 연장선으로 묶으며 편집 행위를 '맥락 복원'이라고 축소한다.

**Tell 제안** (archetype 기반):
- 장태욱(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)
- 한세린(victim_cosplay): 희생 나열(enumeration), 목 잠김(self_reference), 되물음(question_end)

**원본 verbalTells**:
- 장태욱:
  - context_split: "현재 사건과 과거 사건을 지나치게 잘라 설명하며 공통된 패턴 지적을 기술적 우연처럼 만든다."
  - format_attack: "내용 대신 '원본 형식이 아니다', '게시판 절차를 어겼다'를 먼저 공격해 자신의 과거 행위를 뒤로 민다."
  - cool_relabel: "'배제'를 '보호적 분리', '압박'을 '품질 표현 조정'이라고 바꿔 불이익의 강도를 낮춘다."
- 한세린:
  - past_hurt_loop: "현재 증거의 진위를 묻는 질문에도 '그때도 묻혔다'는 과거 상처를 먼저 꺼내 검증 순서를 늦춘다."
  - edit_as_context: "'합성'이 아니라 '맥락을 붙인 것'이라며 편집 행위의 의미를 축소한다."
  - ally_chain: "조사담당, 예전 동료, 외부 감사 이름을 연달아 언급해 자신의 의심이 혼자만의 것이 아니었다고 압박한다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
