# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "family-05",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "family05:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "family-05",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `family-05-v2-atoms.json`
요청 A 결과물

### 파일 2: `family-05-tells-beats.json`
```json
{
  "caseId": "family-05",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `family-05-v3-game-loop-data.json`
```json
{
  "caseId": "family-05",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### family-05

**당사자**:
- a: **박은주** (57세, 동네 미용실 원장, archetype: confrontational)
- b: **박소현** (26세, 웹툰 배경 채색 어시스턴트, archetype: avoidant)

**쟁점 5개**:
- d-1: 은주의 딸 방 출입과 임대인 개입 (truth=true, weight=high)
- d-2: 소현의 공유주거 계약 축소 설명 (truth=true, weight=high)
- d-3: 31초 녹취는 소현의 금전·가출 자백인가 (truth=false, weight=high)
- d-4: 사전 상담 후 계약하기로 한 약속의 쌍방 파기 (truth=true, weight=medium)
- d-5: 임대인의 최종 계약 거절 사유 (truth=true, weight=high)

**증거 6개**:
- e-1: 현관 스마트도어락 출입기록과 방 문열림 알림 [hard] → d-1
- e-2: 임대인 통화녹취와 취소 요청 문자 [soft] → d-1,d-5
- e-3: 공유주거 계약서 초안과 계약금 이체증 [hard] → d-2,d-4
- e-4: 31초 음성클립 파일 [soft] **함정** → d-3
- e-5: 디지털포렌식 감정서와 11분 원본 통화 백업 [hard] → d-3
- e-6: 청년주거상담센터 중재기록과 임대인 회신메일 [hard] → d-4,d-5

**lieConfig**:
- a (3): d-1(LT-2 L2 self_protection), d-3(LT-1 L3 shame_avoidance), d-4(LT-5 L1 relationship_maintenance)
- b (3): d-2(LT-3 L2 face_saving), d-4(LT-6 L1 shame_avoidance), d-5(LT-4 L1 face_saving)

**화법 스타일** (Tell 설계 참고):
- 박은주: 질문을 받으면 사실관계보다 먼저 '엄마가 아니면 누가 막았겠냐'며 보호자 위치를 내세운다. 밀리기 시작하면 딸의 과거 실수와 현재 불안을 한 문장에 묶어, 통제가 아니라 보호였다고 프레임을 바꾼다.
- 박소현: 핵심 답을 바로 하지 않고 계약, 작업 일정, 월수입 순서부터 길게 설명해 본론을 뒤로 미룬다. 억울함이 커지면 '그래서 또 내가 애 취급이잖아'라는 식으로 사실과 감정을 한꺼번에 쏟아낸다.

**Tell 제안** (archetype 기반):
- 박은주(confrontational): 증거 흔들기(echo_repeat), 동기 단정(self_reference), 선택적 인용(echo_repeat)
- 박소현(avoidant): 숫자 정밀화(number_first), 되묻기(question_end), 동선 나열(enumeration)

**원본 verbalTells**:
- 박은주:
  - protective_overclaim: "불리한 개입 질문이 나오면 '내가 막았으니 사고가 안 난 거야'처럼 결과를 자기 보호 논리로 덮는다."
  - public_scold_mode: "목소리가 커지면서 상대를 이름 전체로 부르고, 마치 훈육하듯 날짜와 실수를 차례로 열거한다."
  - fear_reframe: "위조나 침범 지적이 나오면 '무서워서 그랬다'고 감정 이유를 먼저 꺼내 행위의 무게를 줄이려 한다."
- 박소현:
  - scope_narrowing: "불리한 계약 질문이 나오면 '그 시점만 보면 단독으로 보였어'처럼 범위를 잘게 쪼개 전체 오해를 줄이려 한다."
  - late_context_dump: "핵심보다 앞서 월수입, 마감 일정, 방 크기 같은 주변 정보를 길게 풀어 시간을 번다."
  - adult_status_assertion: "궁지에 몰리면 '나는 스물여섯이야'를 반복해 행위 평가보다 나이와 자율성을 먼저 방패로 세운다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
