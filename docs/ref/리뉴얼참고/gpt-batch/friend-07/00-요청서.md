# V3 배치 요청서 — GPT

## 프로젝트 배경

**Solomon**은 LLM 기반 법정 심문 게임입니다. 플레이어는 재판관으로서 양측 당사자(AI NPC)를 심문하여 진실을 밝힙니다.

### 아키텍처
- **엔진이 의미 결정**, LLM은 연기만. 매 턴 `ResponseBlueprint` 생성 → atom 선택 → LLM 1~3문장 응답.
- NPC는 lieState 상태 머신(S0 완전부정 → S5 시인)을 따름.
- **ClaimAtom**: 의미 단위 (사실+슬롯). 기존 publicClaim 문장을 원자로 분해한 것.
- **Slot**: 표현 변형 (exact: "2,800,000원", neutral: "해당 금액", judgeRef: "장모님")
- 질문: fact_pursuit(사실추궁), motive_search(동기탐색), empathy_approach(공감접근), evidence_present(증거제시)

### 이번 요청에서 만들 것
첨부된 사건 원본 JSON을 읽고, 아래 **4가지 데이터**를 한 번에 생성합니다.

---

## 요청 A: ClaimPolicy V2 (atom 기반)

각 파티 × 쟁점 × S0~S5 state마다 ClaimPolicy를 생성합니다.
핵심은 `claimAtoms` 배열 — publicClaim 문장을 원자로 분해.

### atom 분해 규칙
1. publicClaim의 각 문장을 1~2개 atom으로 분해
2. 숫자/날짜/인명은 반드시 slot으로 분리
3. 태그: act, timeline, motive, emotion, rule, evidence, context, identity, responsibility, relationship, counter, self_justification, harm, fear, shame, privacy, admission, denial, uncertainty, quote, threshold, beneficiary, institution, legacy_sentence (24종)
4. S0~S1: deny/hedge용 (핵심 회피), S2~S3: partial/blame (부분 인정+전가), S4~S5: emotional/confess (시인)
5. privateKnowledge → 고 state에서 해금되는 atom의 factText 소재
6. 각 atom에 stanceHints, usableInSubActions 부여

### 출력 구조
```json
{
  "caseId": "friend-07",
  "claimPolicies": {
    "a": {
      "d-1": {
        "S0": {
          "disputeId": "d-1", "state": "S0",
          "publicClaim": ["원문 유지"],
          "privateKnowledge": ["원문 유지"],
          "suppressions": ["원문 유지"],
          "emotionalLeakRisk": "low",
          "tellPool": ["tell_id_1"],
          "claimAtoms": [
            { "id": "friend07:a:d-1:act:0", "factText": "...", "tags": [...], "slots": {...}, "stanceHints": [...] }
          ]
        }
      }
    }
  }
}
```

### atom 예시 (spouse-01에서 발췌)
```json
{
  "id": "spouse01:a:d-1:act:0",
  "factText": "공동계좌에서 이체가 있었다는 사실",
  "tags": ["act", "timeline"],
  "slots": {
    "amount": { "exact": "2,800,000원", "rounded": "280만원", "neutral": "해당 금액" },
    "time": { "dateExact": "9월 20일", "period": "추석 연휴 직전", "neutral": "그 시점" }
  },
  "stanceHints": ["deny", "hedge"],
  "usableInSubActions": ["fact_pursuit", "evidence_present"]
}
```

---

## 요청 B: ExecutableVerbalTell + BeatScript

### Tell (파티당 3개 = 6개)
사건 원본의 `speechStyle`과 `archetype`을 반영한 말버릇 규칙.

```json
{
  "id": "tell_id",
  "appliesWhen": ["lying", "cornered", "emotional", "avoiding", "shame", "hurt", "defensive"],
  "lexicalHooks": ["어휘1", "어휘2"],
  "sentenceShape": "number_first|question_end|enumeration|echo_repeat|conditional|self_reference",
  "cadence": "every_turn|once_every_2_turns|max_once_per_turn|on_trigger_only",
  "originalPattern": "이 말버릇의 설명"
}
```

### Beat (30~40개)
쟁점별 deny/hedge/partial/blame/confession/evidence_hit × 해당 state.

```json
{
  "caseId": "friend-07",
  "party": "a",
  "disputeId": "d-1",
  "beatType": "deny|hedge|partial|blame|confession|evidence_hit",
  "line": "1~3문장, 캐릭터 말투 반영",
  "behaviorHint": "행동/표정 연출 지시",
  "applicableStates": ["S0", "S1"],
  "afterEvidence": "e-1"
}
```

---

## 요청 C: V3 Game Loop

### DossierCard 3개
증거 2개를 묶은 카드. 각 카드에 A 대상 3질문 + B 대상 3질문.

```json
{
  "id": "dossier-1",
  "name": "카드 이름",
  "description": "1줄 설명",
  "evidenceIds": ["e-1", "e-2"],
  "relatedDisputes": ["d-1", "d-3"],
  "subjectParty": "both",
  "challenges": [
    {
      "targetParty": "a",
      "questions": [
        {
          "id": "dossier-1.a.q1",
          "text": "재판관 말투 질문",
          "attackVector": "identity|context|legality|authenticity",
          "onSuccess": { "blockVector": "identity", "revealAtom": "d1.unlock.s2.xxx", "lieAdvance": true }
        }
      ]
    }
  ]
}
```

### StateUnlockAtom
a(해당 쟁점) + b(해당 쟁점) × S2/S3/S4/S5 각 1~2개.
**revealAtom ID와 일치해야 함.**

```json
{
  "id": "d1.unlock.s2.xxx",
  "factText": "새로 드러나는 사실",
  "tags": ["context", "motive"],
  "unlockedAtState": "S2",
  "slots": { "person": { "fullName": "...", "judgeRef": "...", "neutral": "그 사람" } },
  "stanceHints": ["partial", "emotional"]
}
```

### 이벤트 텍스트
- 모순 3~4개, 끼어들기 3개, 감정폭발 양측 각 2개 (총 ~10개)

### TransitionBeat 20~24개
각 파티 × 쟁점 × S1→S2~S4→S5 (4단계).
d-5 같은 쌍방 쟁점은 양측 모두 필요.

---

## 출력: 3개 파일

### 파일 1: `friend-07-v2-atoms.json`
요청 A 결과물

### 파일 2: `friend-07-tells-beats.json`
```json
{
  "caseId": "friend-07",
  "executableTells": { "a": [...], "b": [...] },
  "beatScripts": [...]
}
```

### 파일 3: `friend-07-v3-game-loop-data.json`
```json
{
  "caseId": "friend-07",
  "dossierCards": [...],
  "stateUnlockAtoms": { "a": {...}, "b": {...} },
  "events": { "contradictions": [...], "interjections": [...], "emotionalOutbursts": [...] },
  "transitionBeats": [...]
}
```

각 파일의 TS export 버전도 함께 출력해 주세요.

---

## 이번 배치 대상 사건

### friend-07

**당사자**:
- a: **서진아** (29세, 공연기획 어시스턴트, archetype: cold_logic)
- b: **박성호** (30세, 영어학원 강사, archetype: victim_cosplay)

**쟁점 5개**:
- d-1: 성호의 비밀 연애 은닉과 진아에게 알리바이 요청 (truth=true, weight=high)
- d-2: 진아의 메시지 편집과 다중 채널 확산 (truth=true, weight=high)
- d-3: 성호는 연애 관계를 이용해 리더 선발을 따냈는가 (truth=false, weight=high)
- d-4: 작년 비밀 유지 합의의 쌍방 파기 (truth=true, weight=medium)
- d-5: 성호의 '이미 운영진에게 제한 고지했다' 주장 (truth=false, weight=high)

**증거 6개**:
- e-1: 진아가 양소라와 운영진에게 보낸 편집 캡처 [soft] **함정** → d-2,d-3
- e-2: 진아-성호 원본 메신저 export와 일정 맞바꿈 대화 [hard] → d-1,d-4
- e-3: 작년 소문 사태 뒤 작성한 제한 공개 합의 노션 [hard] → d-4,d-5
- e-4: 연합공연제 리더 선발 점수표와 이해충돌 제외 로그 [hard] → d-3,d-5
- e-5: 진아의 동문 단톡 전달 기록과 친한친구 스토리 보관본 [soft] → d-2,d-4
- e-6: 연습 스케줄 변경표와 택시 영수증, 통화기록 [hard] → d-1

**lieConfig**:
- a (3): d-2(LT-2 L2 self_protection), d-3(LT-4 L2 revenge), d-4(LT-5 L1 shame_avoidance)
- b (3): d-1(LT-2 L2 self_protection), d-5(LT-1 L2 career_preservation), d-4(LT-7 L2 relationship_maintenance)

**화법 스타일** (Tell 설계 참고):
- 서진아: 대화할 때 시간순으로 로그와 말투를 재배열해 상대 진술의 틈을 찾는다. 하지만 배신감을 느끼면 사실 확인과 비난을 한 문단 안에 묶어 말해 방어할 여지를 거의 주지 않는다.
- 박성호: 자신이 얼마나 압박받았는지와 동호회에서 감당한 분위기를 먼저 길게 설명하며 피해자 자리를 선점한다. 몰리면 사실관계보다 '망치려던 게 아니었다'는 의도를 반복해 책임을 흐린다.

**Tell 제안** (archetype 기반):
- 서진아(cold_logic): 영수증 나열(number_first), 짧은 부정(echo_repeat), 건조한 비아냥(self_reference)
- 박성호(victim_cosplay): 희생 나열(enumeration), 목 잠김(self_reference), 되물음(question_end)

**원본 verbalTells**:
- 서진아:
  - timeline_stitch: "불리한 질문이 나오면 시각과 요일을 연속으로 읊으며 핵심 책임보다 흐름 재구성에 몰입한다."
  - term_freeze: "'전달'과 '폭로', '비교'와 '편집' 같은 단어 차이를 붙잡아 행위의 수위를 낮추려 한다."
  - quiet_cutoff: "감정이 올라오면 목소리를 낮춘 채 문장을 아주 짧게 끊어 상대 답변을 중간에서 잘라 버린다."
- 박성호:
  - pressure_preface: "질문을 받으면 먼저 '그때는 다들 예민했고'라고 압박 상황을 설명해 자신의 선택이 불가피했다는 프레임을 만든다."
  - intent_shield: "비밀 은닉을 묻으면 '숨기려던 게 아니라 타이밍을 못 잡은 거야'라고 의도를 먼저 꺼낸다."
  - wounded_pause: "억울함을 강조할 때 말을 멈추고 한숨을 길게 쉰 뒤 상처받은 표정으로 문장을 잇는다."

**DossierCard 권장 묶음**:
- 도시에 1: e-1 + e-2
- 도시에 2: e-3 + e-4
- 도시에 3: e-5 + e-6
(더 나은 묶음이 있으면 자유롭게 조정)


각 사건의 원본 JSON 파일이 첨부되어 있습니다.
**사건별로 각각** 위 3개 파일을 생성해 주세요.
